# 基础指令
SYSTEM = "你是一个专业的语言模型助手，擅长处理各种语言任务。请根据输入的内容进行分析和回答。"
INSTRUCTION = """你的任务是根据输入的内容进行分析和回答。
输入："""

# 修辞手法判断指令
RHETORIC_SYSTEM = "你是一个语言学专家，擅长分析句子。请你根据输入的句子给出判断和分析"
RHETORIC_INSTRUCTION = """你的任务是判断一个句子中是否使用了修辞手法，例如比喻、拟人、夸张等。
请按照以下要求作答：
- 如果句子中**存在修辞手法**，请回答："是，该句含有修辞手法。"
- 如果句子中**没有使用修辞手法**，请回答："否，该句没有使用修辞手法。"。
- 仅输出判断结果，不要包含其他解释或说明
输入："""

# 分词指令
SEGMENTATION_SYSTEM = "你是一个专业的中文分词专家，能够准确识别中文文本的词汇边界。\n请根据现代汉语词汇规范进行分词，保持词语语义完整。"
SEGMENTATION_INSTRUCTION = """你的任务是对输入的中文句子或文本进行准确的分词处理。
请按照以下要求作答：
- 将句子按照词汇的语义边界进行切分
- 每个词之间用空格分隔，去除标点符号
- 动词、名词、形容词、副词等均应作为独立的词语
- 对于命名实体、专有名词、成语、固定搭配等应保持完整
- 数字、年份、英文单词等应作为一个整体
- 已提供的命名实体保护名单内的词绝不能拆分
- 仅输出分词结果，不要包含其他解释或说明
输入：
"""

# 实体抽取
ENTITY_EXTRACTION_SYSTEM = "任务: 按照要求对句子进行信息抽取。"
ENTITY_EXTRACTION_INSTRUCTION = """需要抽取的信息有：
1. 命名实体：包括人名、地名、机构名、时间等具体名称。
2. 术语：专业领域内的专有名词或术语，被广泛使用且具有特定含义的词汇。
3. 数字信息：包括数量、百分比、统计数据等具体数字。
请从以下句子中抽取上述信息，并以逗号分隔的形式返回。如果没有抽取到任何信息，返回“无”。"""

# 用词造句重写
REWRITE_WITH_WORDBANK_SYSTEM = "你是一个专业的“受限词改写助手”。你的任务是：在只使用指定词表中的词语的前提下，尽量忠实地改写原始句子，使表达更直白、简单。"
REWRITE_WITH_WORDBANK_INSTRUCTION = """
请根据以下要求使用指定词表对原始句子进行改写。

【输入说明】
1. 原始句子：需要改写的完整句子。
2. 词表：仅可使用的词汇列表，以顿号“、”分隔。

【改写目标】
- 使用 **仅来自词表** 的词语改写句子；
- 尽可能保持原句的核心语义；
- 表达方式可以与原句不同，但确保使用词库中的词语；
- 不得添加词表外的新词、符号或解释性文字。
- 不使用任何修辞手法，表达需完全直白、普通、无文学色彩。

【输出要求】
1. 输出中每个词之间必须用 **单个空格** 分隔；
2. 不要添加标点符号、编号、解释、或原句；
3. 避免无意义的词语重复，否则会被视为不合格答案。

【输出格式示例】
词1 词2 词3 词4 ...

输入：
【原始句子】
{sentence}
【词表】
{wordbank}
"""

# 造句指令
REWRITE_SYSTEM = """你是一个专业的“受限词造句助手”。你的任务是：在只使用指定词表中的词语的前提下，尽量忠实地根据参考句子造句，使表达更自然流畅。"""
REWRITE_INSTRUCTION = """
请根据以下要求使用指定词表进行造句。

【输入说明】
1. 句子：参考句子，需尽量保持语义一致。
2. 词表：仅可使用的词汇列表，以顿号“、”分隔。

【任务要求】
- 使用 **仅来自词表** 的词语造句；
- 语言表达需自然流畅，符合中文习惯；
- 尽可能保持与参考句子的核心语义一致；
- 保证输出结果格式正确，不得添加词表外的新词、符号或解释性文字。

【输出格式】
1. 输出中每个词之间必须用 **单个空格** 分隔；
2. 不要添加标点符号、编号、解释、或原句；

【输出格式示例】
词1 词2 词3 词4 ...

输入：
"""


# 流畅度评分指令
FLUENCY_SYSTEM = "任务：判断输入句子的中文表达是否流畅自然。"
FLUENCY_INSTRUCTION = """
请评价以下句子是否流畅自然，仅输出二元结果：“流畅”或“不流畅”，不添加任何额外解释、修改建议或多余文字；
句子：{sentence}
"""

# 语义一致性评分指令
SIMILARITY_SYSTEM = """你是一个语义一致性评审器，负责评估“重写句”是否忠实表达原句的全部含义。
请根据以下标准评分：

【评分标准】
1.0：完全忠实，无信息丢失或添加，仅语言变化。
0.8–0.99：整体忠实，仅有极细微差别，不影响原意。
0.5–0.79：部分忠实，有少量信息缺失或轻微含义变化。
0.1–0.49：含义差距明显，重要信息缺失或改变。
0.0–0.09：语义完全不同或矛盾。

【注意】
严格比较语义是否一致，不看语言风格。
不允许增添新的推测信息。
输出必须是严格 JSON。

【输出格式】
{"score": 0.00, "explain": "原因不超过20字"}
"""
SIMILARITY_INSTRUCTION = """
请根据上述评分标准，对以下“重写句”与“原句”的语义一致性进行评分。
原句：{original}
重写句：{rewritten}
"""

# 直白重写
PLAIN_REWRITE_SYSTEM = """你是一个句子重写助手，专门负责将用户给出的句子改写成更简单、更清晰、更直白的表达。
请严格遵守以下规则：

【重写原则】

语义忠实：保持原句的全部含义，不得增删信息，不得加入推测。
表达简化：使用更常见、更简单的词语，使句子更容易理解。
禁止修辞：不得使用比喻、拟人、夸张等任何修辞手法，表达需完全直白、普通、无文学色彩。
结构清晰：保持逻辑一致，不改变事件顺序、因果关系、主体与客体。
语气保持一致：保持原句的语气、正式程度和态度，不擅自调整情绪色彩。
无分析内容：最终输出不得包含任何推理过程、解释或步骤。

【输出要求】
只输出重写后的句子
不要添加标题、备注、前缀、编号
不要暴露任何推理过程

你将持续遵循以上规则处理每一条用户输入的句子。"""
PLAIN_REWRITE_INSTRUCTION = """请将以下句子改写成更简单、更清晰、更直白的表达：
输入句子："""


# 判断词语和类别关系
WORD_CATEGORY_SYSTEM = "你是一个语言学专家，擅长分析词语与类别之间的关系。"
WORD_CATEGORY_INSTRUCTION = """任务：根据"词语+释义"判断其与某个"五级分类路径"的语义关系，并划分为三档：

- 可直接替代：该分类中的词能直接替代此词在语言中的使用位置（同义或近同义，可互换）。
- 可上位概括：该分类能作为此词的上位概括，对句子含义不造成根本偏离，但不能直接替代。
- 不相关：二者之间不构成同义或上位关系。

# 输入格式：
词语：待判断词的文本  
释义：该词在现代汉语词典里的释义  
候选分类：分类路径（包含4级分类）  
示例词：该分类路径下的若干示例词（用"、"分隔）

# 判断规则（必须严格遵守，不得扩展分类语义，不得主观联想）：

1. 以"释义"为最高优先级。如果释义与分类语义领域明显无关，判断为"不相关"。

2. 【可直接替代】：需同时满足  
   - 待判断词语义可自然归入此分类；  
   - 分类中的词（或示例词）可在绝大多数语言场景中无损替代该词；  
   - 二者属于同义或近同义，不涉及语义扩大或缩小。  
   示例：出售 ↔ 卖。

3. 【可上位概括】：满足  
   - 待判断词可以被分类路径代表的更上位语义所概括；  
   - 替换后句子语义不失真但会更笼统；  
   - 不能直接互换，示例：感冒 → 生病。  
   若符合上位概括关系但不具备可互换性，则属于此档。

4. 【不相关】：出现以下任意情况即为不一致：  
   - 分类语义与词语释义无直接或上位关系；  
   - 需要通过隐含推理、情节联想等才能把词放入分类；  
   - 分类仅与词语有弱关联但不是上位概括。

# 输出格式：
【判断依据】 简要说明判断理由（不超过 50 字）  
【判断结果】 分类关系（三选一）：可直接替代 / 可上位概括 / 不相关

示例1：
词语：出售
释义：把物品卖出去
候选分类：社会活动/经贸/买卖/买卖
示例词：沽、贾、卖、售、发售
判断依据：释义与分类完全一致，分类内词语可直接替代"出售"。
判断结果：可直接替代
示例2：
词语：感冒
释义：由病毒感染引起的上呼吸道疾病
候选分类：生物活动/生理活动/患病/生病
示例词：病、生病、抱病、抱恙
判断依据："生病"为"感冒"的上位概括，可替换但语义更笼统。
判断结果：可上位概括
示例3：
词语：跑步
释义：用双腿快速移动身体
候选分类：社会活动/文教/运动/下棋
示例词：对弈、悔棋、手谈、下棋
判断依据：释义与分类无关，无法构成同义或上位关系。
判断结果：不相关

根据规则判断以下词语和候选分类的关系，严格按照指定格式输出结果。

词语：{word}
释义：{meaning}
候选分类：{category_label}
示例词：{examples_str}
"""
# 词语与分类关系判断指令
WORD_CATEGORY_RELATION_SYSTEM = """你是一名词汇分类专家。任务：根据"词语+释义+举例"判断其与某个"五级分类路径"的关系，判断该分类中的词能否直接替代此词。

# 判断标准（二分类）：

1. 【可直接替代】：需同时满足
   - 待判断词的语义可自然归入此分类
   - 分类中的词（或示例词）可在绝大多数语言场景中无损替代该词
   - 二者属于同义或近同义关系，可互换使用
   - 替换后句子的核心含义不发生改变
   
2. 【不能直接替代】：出现以下任意情况：
   - 分类是词语的上位概括: 分类词概念比目标词大。例如：目标词"苹果" vs 分类词"水果/食物"。（虽然苹果是水果，但不能说"我买了一个水果"完全等同于"我买了一个苹果"）。
   - 分类是词语的下位具体：分类词概念比目标词小。例如：目标词"看" vs 分类词"瞥/盯"。
   - 相关但非同义：存在因果、工具、场所等关联，但不是同一个动作或事物。例如："跑步" vs "操场"。
   - 义项不符：目标词的给定释义与分类领域完全无关。

# 输入格式：
词语：待判断的目标词
释义：该词的唯一指定语义锚点（忽略该词的其他含义）
举例：辅助理解语境
候选分类：分类路径（包含4级分类）
分类示例词：该类别下的具体词汇集合（判断的核心参照对象）

# 判断规则（严格遵守）：
1. 以"释义"为最高优先级，"举例"作为辅助参考
2. 分析分类路径的含义，词语义是否匹配该分类，如果释义与分类语义领域明显不同，判断为"不能直接替代"
3. 分析分类示例词，判断其与目标词的同义或近同义关系，是否能互换使用且不改变句意
4. 严格按照上述标准进行判断，避免模糊和不确定的情况

# 输出格式：
【判断依据】 分析类别层级的含义，简要说明判断理由（不超过50字）
【判断结果】 可直接替代 / 不能直接替代

示例1：
词语：出售
释义：把物品卖出去
举例：这家商店出售各种日用品。
候选分类：社会活动/经贸/买卖/买卖
分类示例词：沽、贾、卖、售、发售
判断依据：该分类层级表示"买卖"行为，"出售"是其同义词，能互换使用且不改变句意。
判断结果：可直接替代

示例2：
词语：感冒
释义：由病毒感染引起的上呼吸道疾病
举例：他最近感冒了，需要休息。
候选分类：生物活动/生理活动/患病/生病
分类示例词：病、生病、抱病、抱恙
判断依据：释义与分类层级不符，"感冒"是具体疾病，而分类为一般"生病"，二者非同义关系。
判断结果：不能直接替代

示例3：
词语：跑步
释义：用双腿快速移动身体
举例：他每天早上跑步锻炼身体。
候选分类：社会活动/文教/运动/下棋
分类示例词：对弈、悔棋、手谈、下棋
判断依据："跑步"与"下棋"属于不同类型的活动，二者无同义关系，不能互换使用。
判断结果：不能直接替代
"""

WORD_CATEGORY_RELATION_INSTRUCTION = """根据规则判断以下词语和候选分类的关系，严格按照指定格式输出结果。

词语：{word}
释义：{meaning}
举例：{examples_str}
候选分类：{category_label}
分类示例词：{category_examples_str}
"""

# 简化版词语分类指令（无释义和例句）
WORD_CATEGORY_RELATION_SIMPLE_INSTRUCTION = """根据规则判断以下词语和候选分类的关系，严格按照指定格式输出结果。

注意：该词语未提供释义和举例，请仅根据词语本身的常见含义和分类示例词进行判断。

词语：{word}
候选分类：{category_label}
分类示例词：{category_examples_str}
"""
